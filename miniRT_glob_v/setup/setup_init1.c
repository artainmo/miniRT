#include "../miniRT.h"

/*void ft_sp_init(void)
{
  if (!(s->sp = malloc(sizeof(t_sp))))
    ft_exit_no_free("ERROR\nFailed malloc\n");
  s->sp.r = 0;
  s->sp.first = s->sp;
}

void ft_pl_init(void)
{
  if (!(s->pl = malloc(sizeof(t_pl))))
    ft_exit_no_free("ERROR\nFailed malloc\n");
  s->pl.ndir.x = 0;
  s->pl.ndir.y = 0;
  s->pl.ndir.z = 0;
  s->pl.first = s->pl;
}

void ft_sq_init(void)
{
  if (!(s->sq = malloc(sizeof(t_sq))))
    ft_exit_no_free("ERROR\nFailed malloc\n");
  s->sq.side = 0;
  s->sq.first = s->sq;
}

void ft_cy_init(void)
{
  if (!(s->cy = malloc(sizeof(t_cy))))
    ft_exit_no_free("ERROR\nFailed malloc\n");
  s->cy.r = 0;
  s->cy.first = s->cy;
}

void ft_tr_init(void)
{
  if (!(s->tr = malloc(sizeof(t_tr))))
    ft_exit_no_free("ERROR\nFailed malloc\n");
  s->tr.c.red = 0;
  s->tr.c.green = 0;
  s->tr.c.blue = 0;
  s->tr.first = s->tr;
}*/
