#include "../miniRT.h"

/*t_sp *ft_sp_new(void)
{
  t_sp *new;

  if (!(new = malloc(sizeof(new))))
    ft_exit_no_free("ERROR\nFailed malloc\n");
  new->r = 0;
  return (new);
}

t_pl *ft_pl_new(void)
{
  t_pl *new;

  if (!(new = malloc(sizeof(new))))
    ft_exit_no_free("ERROR\nFailed malloc\n");
  new->ndir.x = 0;
  new->ndir.y = 0;
  new->ndir.z = 0;
  return (new);
}

t_sq *ft_sq_new(void)
{
  t_sq *new;

  if (!(new = malloc(sizeof(new))))
    ft_exit_no_free("ERROR\nFailed malloc\n");
  new->side = 0;
  return(new);
}

t_cy *ft_cy_new(void)
{
  t_cy *new;

  if (!(new = malloc(sizeof(new))))
    ft_exit_no_free("ERROR\nFailed malloc\n");
  new->r = 0;
  return (new);
}

t_tr *ft_tr_new(void)
{
  t_tr *new;

  if (!(new = malloc(sizeof(new))))
    ft_exit_no_free("ERROR\nFailed malloc\n");
  new->c.red = 0;
  new->c.green = 0;
  new->c.blue = 0;
  return (new);
}*/
